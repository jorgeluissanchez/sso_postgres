/**
 * Placeholder. Real implementation arrives in task #75 (CRUD
 * pages). For now we render a heading so the routing tree compiles
 * and the auth flow can be exercised end-to-end.
 */
export function UsersListPage() {
  return (
    <section>
      <h1 className="text-xl font-semibold text-slate-900">Usuarios</h1>
      <p className="mt-2 text-sm text-slate-500">
        Listado y administración de usuarios (pendiente #75).
      </p>
    </section>
  );
}
