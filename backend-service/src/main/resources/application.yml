spring:
  application:
     name: DQ_SABOREAPP

eureka:
  client:
    enabled: true
    serviceUrl:
      defaultZone: http://localhost:9761/eureka/
  instance:
    hostname: localhost
    nonSecurePort: 9183
    
server:
  port: 9183